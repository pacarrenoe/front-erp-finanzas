export function TransactionsPage() {
  return (
    <section aria-labelledby="tx-title">
      <h2 id="tx-title">Movimientos</h2>
      <p>Acá irá el CRUD de ingresos/egresos con filtros.</p>
    </section>
  );
}